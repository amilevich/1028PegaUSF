package com.example.constructors;

public class MyClass {
	/*
	 * what is a constructor?
	 * a block of code that instantiates a newly created object
	 * anytime an object is created it calls the constructor
	 * it is not a method because the do not have a return type and cannot be overriden
	 * cannot be abstract, final or static
	 * must be named the same as the class as it wants to
	 * */
	MyClass(){
		//three types of constructors
		/*
		 * 1) default - if no constructor is explicitly provided, the jvm provides you with a 
		 * no argument constructor be default
		 * 
		 * 2) no args -  constructor with no arguments/parameters. the signature of this no arg constructor
		 * is the same as the default one but the body can have any code unlike the default constructors
		 * 
		 * 3) parameterized - constructor with arguments or parameters
		 * */
	
		
		
	}
	public static void main(String[] args) {
		Tiger tiger2 = new Tiger(true , 6, 4, "John");
		
		System.out.println(tiger2);
		
	}
}
