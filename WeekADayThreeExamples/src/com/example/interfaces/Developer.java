package com.example.interfaces;

public class Developer implements Person{

	@Override
	public void walk() {
		System.out.println("hey im walking here");
		
	}
	
	/*
	 * if you are inheriting an interface form a class the keyword is implements
	 * 
	 * if you inherit an interface form an interface the keyworkd is extends
	 * 
	 * if you are inheriting a class from a class the keyword is extends
	 * */
	
}
