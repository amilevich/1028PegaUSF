package com.project1.logger;

//import org.apache.log4j.LogManager.getRootLogger();
import java.util.logging.LogManager;
//import java.util.logging.Logger;

import org.apache.log4j.Logger;


public final class LogFunc {
	public static final Logger log = Logger.getLogger(LogFunc.class);	
}
// LogFunc.logger.info(obj.getId() + " " + obj.getStatusId());
// LogFunc.logger.info(obj.getId() + " " + obj.getStatusId());
// 